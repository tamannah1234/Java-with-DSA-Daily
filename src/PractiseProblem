/******************************************************************************

                            Online Java Compiler.
                Code, Compile, Run and Debug java program online.
Write your code in this editor and press "Run" button to execute it.

*******************************************************************************/
/*
You are given an integer array nums and an integer k.
👉 Task:
Return true if there exist two distinct indices i and j such that:


nums[i] == nums[j]


|i - j| <= k


Otherwise, return false.
📌 Example:
nums = [1,2,3,1], k = 3 → true
nums = [1,2,3,1], k = 2 → false

Question 2 (Strings)
Given a string s, find the first non-repeating character and return its index.
If it doesn’t exist, return -1.
📌 Example:
s = "leetcode" → 0
s = "loveleetcode" → 2

Question 3 (Binary Search / Math)
Given a sorted array of integers and a target value x, return the index where x should be inserted to keep the array sorted.
📌 Example:
nums = [1,3,5,6], x = 5 → 2
nums = [1,3,5,6], x = 2 → 1
nums = [1,3,5,6], x = 7 → 4

(Binary search and at the end return low)

Your turn 👇
Answer one by one:
1️⃣ Explain approach for Question 1
(Then I’ll interrupt like an interviewer 😈, ask follow-ups, and judge)
Whenever you’re ready — start with Q1.Is this conversation helpful so far?*/

import java.util.*;
public class Main
{
//     public static int firstNonRepeatingChar(String s){
//     /*s = "leetcode" → 0
// s = "loveleetcode" → 2*/
//     Map<Character,Integer> map = new HashMap<>();
//     int indx=0;
    
//     for(int i=0;i<s.length();i++){
//         char ch = s.charAt(i);
        
//         if(map.containsKey(ch)){
//             map.put(ch,map.getOrDefault(ch,0)+1);
//         }
//         map.put(ch,indx++);
        
         
//     }
    
//     for(Map.Entry<Charater,Integer> e = map.entrySet()){
//         if(e.getKey())
//     }
      
//     }
    
// 	public static void main(String[] args) {
// 		System.out.println("Hello World");
// 	}
// }


//Search Insert Position (Binary Search)
public static int insertAtPos(int arr[],int k){
 int low=0;int high =arr.length-1;
 while(low<=high){
     int mid = low + (high-low)/2;
     if(arr[mid]==k){
         return mid;
     }else if(arr[mid]>k){
         high = mid-1;
     }else{
         low=mid+1;
     }
   
 }
      return low;
    
}

public static int firstUniqueChar(String s){
    
    Map<Character,Integer> freq = new HashMaop<>();
    //1st pass: count freq
    for(char ch : s.toCharArray){
      freq.put(ch,freq.getOrDefault(ch,0)+1);
    }
    //2nd pass : Itearte and return unique index
    for(int i=0;i<s.length();i++){
      freq.get(s.charAt(i)==1){
          return i;
      }  
    }
    return -1;
    
}

 public boolean containsNearbyDuplicate(int[] nums, int k) {
        Map<Integer, Integer> map = new HashMap<>();

        for (int i = 0; i < nums.length; i++) {
            if (map.containsKey(nums[i])) {
                if (i - map.get(nums[i]) <= k) {
                    return true;
                }
            }
            // update last seen index
            map.put(nums[i], i);
        }
        return false;
    }
